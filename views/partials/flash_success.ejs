<%# The flash is an array, so by testing for success.length, you're making sure the flash array is not empty. %> 
<%# Otherwise, testing just for success will bring up an empty array. %> 

<% if (success && success.length) { %>
    <div class="flash flash__success">
        <p><%= success %></p> 
        <button class="close__flash"><svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
          </svg></button>
    </div>
<% } %>